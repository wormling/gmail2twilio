# gmail2twilio - Forward a gmail folder/label to your SMS enabled twilio number.

## Configuration:
* install nodejs (I also install and use nodemon)
* copy config.dist.js to config.js and edit the settings
* nodemon index
